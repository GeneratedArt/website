1. Non-Negotiable Tech Stack
LayerTechnologyPurposeStatic public siteJekyll 4.x built by GitHub Actions, served by Cloudflare PagesLanding, artist directory, project catalog, gallery partner pages, blog, docsDynamic appCloudflare Workers (TypeScript, wrangler)All API endpoints, auth, GitHub orchestration, mint indexingRelational storeCloudflare D1 (SQLite)Users, artists, projects, editions, sales, galleriesKV storeCloudflare KVSessions, rate-limit counters, feature flagsObject storageCloudflare R2Thumbnails, preview captures, high-res renders, signed uploadsQueuesCloudflare QueuesRender/capture jobs, IPFS pin jobs, webhook retriesEthereum RPCCloudflare Web3 Ethereum GatewayRead-only chain queries from WorkersIPFS gatewayCloudflare Web3 IPFS Gateway (DNSLink subdomain, e.g. ipfs.generatedart.com)Serve artist bundles and token metadataIPFS pinningPinata or web3.storage (Cloudflare gateway does NOT pin — do not skip this)Persistent storage of artist code bundles and metadataSource of truth for codeGitHub (org GeneratedArt, one repo per project under GeneratedArt/art-<slug>)Artist code, reviews via PR, immutability via tagsCIGitHub ActionsJekyll build, Worker deploy, smart-contract tests, project bundle validationWalletWalletConnect v2 + viem + wagmi (frontend)User-side signing; platform never holds keysSmart contractsSolidity 0.8.24, Foundry for tests, deployed to Base mainnet (L2, cheap) + Base Sepolia for testnetMinting, royalties (EIP-2981), primary/secondary splitFrontend framework for dynamic pagesAstro islands or Preact + HTMX served from Cloudflare Pages (NOT Next.js, NOT Vercel)Keeps the static-first postureRendering sandboxA static renderer.generatedart.com subdomain serving token HTML from IPFS inside an isolated <iframe sandbox>Security boundary for untrusted artist code
Explicit non-goals: no AWS, no Vercel, no Firebase, no Supabase, no MongoDB, no Node.js server, no Rails, no Python backend, no Stripe-as-primary-rails (Stripe is optional for fiat on-ramp to the gallery). Do not reach for any of these even if they feel easier.